version: "2"
sql:
  - engine: "mysql"
    schema: "db/migrations"
    queries: "db/queries"
    gen:
      go:
        package: "db"
        out: "internal/repository/db"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
